/*
facewall program takes event checkin guest, displays photo of guest in an html page, and can randomly select guests based on rules using api.

#### import data
    facewall < import.csv

csv first row should be headers same as table field name, only following fields are used:

    code, name, group, imgUrl

####

*/

package main
